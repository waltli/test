#############\u6587\u4ef6\u4e0a\u4f20#########
fs.dir=/home/syk/work/fs
fs.temp.dir=${fs.dir}/temp
fs.formal.dir=${fs.dir}/formal

fs.mapping=/fs
fs.temp.mapping=${fs.mapping}/temp
fs.formal.mapping=${fs.mapping}/formal

#icon.dir=${fs.formal.dir}/icon
#poster.dir=${fs.formal.dir}/poster
#photo.dir=${fs.formal.dir}/photo
#shot.dir=${fs.formal.dir}/shot
#torrent.dir=${fs.formal.dir}/torrent
#
#icon.temp.dir=${fs.temp.dir}/icon
#poster.temp.dir=${fs.temp.dir}/poster
#photo.temp.dir=${fs.temp.dir}/photo
#shot.temp.dir=${fs.temp.dir}/shot
#torrent.temp.dir=${fs.temp.dir}/torrent

bucket.name=syk-1253786918

bucket.host=https://syk-1253786918.cos.ap-chengdu.myqcloud.com

gm.home=/opt/GraphicsMagick